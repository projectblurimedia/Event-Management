# MS Wedding Planner — Frontend

React + Vite + TypeScript + Tailwind CSS public site and admin dashboard.

See the [project root README](../README.md) for full setup instructions, environment variables, folder structure, API reference, and roadmap.

Quick start:

```bash
npm install
cp .env.example .env
npm run dev
```
